const router = require('express').Router();

const apiRoutes = require('./api/user.api.routes')

//const htmlRoutes = require('./api/html/')

route.use('/api.user', apiRoutes);

modual.export = router